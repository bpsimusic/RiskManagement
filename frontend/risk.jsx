import React from 'react';
import ReactDOM from 'react-dom';
import Table from "./congrats";

document.addEventListener("DOMContentLoaded", () => {
	const root = document.getElementById("root");
	ReactDOM.render(<Table/>, root);
});
